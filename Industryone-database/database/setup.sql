-- Run this once in PostgreSQL as a database administrator.
-- The application itself will create/update the tables using JPA.

CREATE DATABASE industryone;

-- Then connect to the industryone database and let Spring Boot
-- create these tables automatically.
